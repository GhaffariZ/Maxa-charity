<?php declare(strict_types=1); require_once __DIR__.'/_guard.php'; dash_require('events'); dash_require_hq(); csrf_check(); $id=(int)($_GET['id']??0); if($_SERVER['REQUEST_METHOD']!=='POST'){ $token=csrf_token(); echo '<form method="post"><input type="hidden" name="csrf_token" value="'.e($token).'"><button>تأیید حذف</button></form>'; exit; } if($id) dash_pdo()->prepare('DELETE FROM events WHERE id=?')->execute([$id]); header('Location: event-list.php');
