# Sensitive — never commit these
config.php
.env
wp-config.php
# Database credentials used by the deploy import script (keep on server only)
database/db.conf
# OS / editor junk
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
# Dependencies (if you use them)
/vendor/
/node_modules/
# Untracked app folder
/public_html/eo/