-- ============================================================================
--  MACSA Charity — Database dump
-- ============================================================================
--
--  ⚠️  این فایل یک placeholder است. آن را با خروجی واقعی دیتابیس جایگزین کن.
--
--  چطور؟  (phpMyAdmin)
--    1) دیتابیس erfantey_macsacharity را انتخاب کن.
--    2) تب Export → Custom → فرمت SQL.
--    3) گزینه‌ی "Add DROP TABLE statement" را تیک بزن.
--    4) Go → فایل دانلودشده را به جای همین فایل بگذار (با همین نام: schema.sql).
--
--  یا با خط فرمان:
--    mysqldump --user=USER --password --add-drop-table erfantey_macsacharity \
--      > database/schema.sql
--
--  توضیحات کامل در database/README.md
-- ============================================================================
